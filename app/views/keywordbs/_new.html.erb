<%= form_for(keywordb, url: keywordbs_path) do |f| %>

    <%= f.label :keywordb  %>
    <%= f.text_field :keywordb %>

      <%= f.submit " 增加第二步斷詞 ", class: "btn btn-large btn-primary" %>
    <% end %>